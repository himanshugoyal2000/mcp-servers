{
  "name": "team-directory-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "Team Directory MCP server — deployed on Vercel as a Streamable HTTP endpoint",
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
